apply plugin: 'java'

ext {
    artifact = 'mvp-compiler'
}

dependencies {
    compile project(':mvp-api')
    compile deps.javapoet
    compile deps.googleauto
}

sourceCompatibility = JavaVersion.VERSION_1_8
targetCompatibility = JavaVersion.VERSION_1_8

apply from: '../install.gradle'
